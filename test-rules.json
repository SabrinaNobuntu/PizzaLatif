{
  "rules": [
    {
      "category": "frontend",
      "pattern": "^fontes/frontend/",
      "cmd": "cd fontes/frontend && npm ci && npm test -- --no-watch"
    },
    {
      "category": "backend",
      "pattern": "^fontes/backend/",
      "cmd": "cd fontes/backend && npm ci && npm test"
    }
  ]
}
