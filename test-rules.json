{
  "rules": [
    { "pattern": "^fontes/frontend/", "cmd": "cd fontes/frontend && npm ci && npm test -- --watchAll=false" },
    { "pattern": "^fontes/backend/", "cmd": "cd fontes/backend && npm ci && npm test" },
    { "pattern": "\\.py$", "cmd": "pip install -r requirements.txt && pytest -q" }
  ]
}
