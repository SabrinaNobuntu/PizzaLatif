{
  "rules": [
    {
      "category": "frontend",
      "pattern": "^fontes/frontend/",
      "cmd": "npm test -- --projects=fontes/frontend"
    },
    {
      "category": "backend",
      "pattern": "^fontes/backend/",
      "cmd": "npm test -- --projects=fontes/backend"
    }
  ]
}
