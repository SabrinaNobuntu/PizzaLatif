{
  "rules": [
    {
      "category": "frontend",
      "pattern": "^fontes/frontend/",
      "cmd": "npm test -- --projects=frontend"
    },
    {
      "category": "backend",
      "pattern": "^fontes/backend/",
      "cmd": "npm test -- --projects=backend"
    }
  ]
}