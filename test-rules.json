{
  "rules": [
    {
      "category": "frontend",
      "pattern": "^fontes/frontend/",
      "cmd": "npx jest -- --projects=fontes/frontend"
    },
    {
      "category": "backend",
      "pattern": "^fontes/backend/",
      "cmd": "npx jest --projects=fontes/backend"
    }
  ]
}
