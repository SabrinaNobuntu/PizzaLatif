// Exemplo de teste simples para frontend

describe('Teste de exemplo do frontend', () => {
  it('deve somar corretamente', () => {
    const resultado = 2 + 2;
    expect(resultado).toBe(4);
  });
});
